<div class="well well-sm well-snippet" id="<%= dom_id(experience) %>">
  <div class="snippet-name" data-id="<%= experience.id %>"> <%= experience.name %> </div>
  <ul>
    <% experience.properties.to_hash.each do |prop, value| %>
      <% unless value == "" || value == nil %>
        <li><span class="snippet-title"><%= prop.capitalize %></span>: <%= value %></li>
      <% end %>
    <% end %>
  </ul>
  <div class="snippet-controls">
    <%= link_to 'Edit', sti_snippet_path(experience.type, experience.id, :edit),
                        remote: true,
                        data: {toggle: "modal", target: "#edit-snippet"},
                        class: "btn btn-xs btn-warning" %>
    <%= link_to 'Delete', sti_snippet_path(experience.type, experience.id),
                          method: :delete,
                          data: { confirm: 'Are you sure?' },
                          class: "btn btn-xs btn-danger",
                          remote: true %>
  </div>
</div>
