<div class = "panel panel-default">
  <div class = "panel-heading">
    <h4 class = "panel-title">
      <a data-toggle = "collapse" data-parent = "#accordion" href = "#collapseNine">
        Skill Snippets
      </a>
    </h4>
  </div>

  <div id = "collapseNine" class = "panel-collapse collapse">
    <div class="add_new_button">
      <%= link_to 'Add new Skill', new_skill_path, class: "btn btn-block btn-info" %>
    </div>
    <div class="panel-body">
      <% @skills.each do |skill| %>
        <div class="well well-sm" draggable="true" id="<%= dom_id(skill) %>">
          <h3> <%= skill.name %> </h3>
          <ul>
            <% skill.properties.to_hash.each do |prop, value| %>
              <% unless value == "" || value == nil %>
                <li><span class="snippet-title"><%= prop.capitalize %></span>: <%= value %></li>
              <% end %>
            <% end %>
          </ul>
          <div class="snippet-controls">
            <%= link_to 'Edit', sti_snippet_path(skill.type, skill.id, :edit), class: "btn btn-xs btn-warning" %>
            <%= link_to 'Delete', sti_snippet_path(skill.type, skill.id),
                                  method: :delete,
                                  data: { confirm: 'Are you sure?' },
                                  class: "btn btn-xs btn-danger",
                                  remote: true %>
          </div>
        </div>
      <% end %>
    </div>
  </div>

</div>
