<div class = "panel panel-default">
  <div class = "panel-heading">
    <h4 class = "panel-title">
      <a data-toggle = "collapse" data-parent = "#accordion" href = "#collapseFour">
        Experience Snippets
      </a>
    </h4>
  </div>

  <div id = "collapseFour" class = "panel-collapse collapse">
    <div class="add_new_button">
      <%= link_to 'Add new Experience', new_experience_path, class: "btn btn-block btn-info" %>
    </div>
    <div class="panel-body">
      <% @experiences.each do |experience| %>
        <div class="well well-sm" draggable="true" id="<%= dom_id(experience) %>">
          <h3> <%= experience.name %> </h3>
          <ul>
            <% experience.properties.to_hash.each do |prop, value| %>
              <% unless value == "" || value == nil %>
                <li><span class="snippet-title"><%= prop.capitalize %></span>: <%= value %></li>
              <% end %>
            <% end %>
          </ul>
          <div class="snippet-controls">
            <%= link_to 'Edit', sti_snippet_path(experience.type, experience.id, :edit), class: "btn btn-xs btn-warning" %>
            <%= link_to 'Delete', sti_snippet_path(experience.type, experience.id),
                                  method: :delete,
                                  data: { confirm: 'Are you sure?' },
                                  class: "btn btn-xs btn-danger",
                                  remote: true %>
          </div>
        </div>
      <% end %>
    </div>
  </div>

</div>
